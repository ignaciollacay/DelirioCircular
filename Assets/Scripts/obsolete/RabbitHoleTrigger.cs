using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RabbitHoleTrigger : MonoBehaviour
{
    public float[] rabbitHoles = new float[3];
    private RabbitHoleTarget rabbitHoleScript;
    /*
    void Start()
    {
        Game = GameObject.Find("RabbitHoleFloor");
        rabbitHoleScript = rabbitHoleFloor.GetComponent<RabbitHoleTarget>();

        rabbitHoleMesa = GameObject.Find("RabbitHoleMesa");
        rabbitHoleScript = rabbitHoleMesa.GetComponent<RabbitHoleTarget>();

        rabbitHoleCama = GameObject.Find("RabbitHoleCama");
        rabbitHoleScript = rabbitHoleCama.GetComponent<RabbitHoleTarget>();
    }

    private void OnTriggerEnter(Collider other)
    {
        if (other.tag == "Player")
        {
            rabbitHoleScript.GetComponent<Collider>().enabled = false;
        }
    }
 
    void rHoles()
    {
        rabbitHoles[0] = Find "RabbitHoleFloor");

    }
       */
}
